# Proxy Vite → API local (padrão em npm run dev)
VITE_API_URL=/api

# Chave da API (obrigatória se API_KEY estiver no .env da raiz).
# Em dev, o Vite também lê API_KEY do .env na raiz do repositório.
# VITE_API_KEY=mesma-chave-do-servidor
